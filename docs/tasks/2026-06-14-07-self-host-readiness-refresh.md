# Task 07: Self-Hosting Readiness Refresh

## Goal

Make `docs/self-hosting-readiness.md` a current operating protocol for
supervised Codex self-development, not a stale snapshot that contradicts the
code.

## Scope

- Update the verdict after admin auth, review cards, provider smoke UI, prompt
  profile UI, and pricing-profile work.
- Separate what is ready for supervised Codex loops from what still blocks
  unattended autonomous loops.
- Keep commands short and directly runnable on the Ubuntu/WSL install.
- Remove stale claims such as "admin auth is missing" and "future review card".

## Definition Of Done

- The document describes the current safe operating mode.
- Remaining gaps are only real manual validation or explicitly unfinished code
  paths.
- The task is committed separately.

## Progress

- [x] Scope captured.
- [x] Document updated.
- [x] Committed separately: `5590a4f Refresh self-host readiness plan`.
